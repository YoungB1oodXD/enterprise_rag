DASHSCOPE_API_KEY=your-dashscope-api-key-here
# JWT 签名密钥（必填，应用启动时检查，无默认值）
# JWT_SECRET_KEY=your-jwt-secret-key
# ES 和数据库密码（可选，不设置时使用 config.yaml 中的默认值）
# ES_PASSWORD=your-es-password
# DB_PASSWORD=your-db-password
